<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 专注计时结束时的震动提醒 -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Android 13+ 发送计时完成通知需要运行时授权 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 关机重启后重新排一遍待办提醒（闹钟会被系统清空） -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.DailyBook">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:theme="@style/Theme.DailyBook">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- 长按图标直达：记一笔 / 加待办 / 开始专注 -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- 待办到点提醒：闹钟到点后重新查库再决定要不要提醒 -->
        <receiver
            android:name=".notify.TodoReminderReceiver"
            android:exported="false" />

        <!-- 每晚记账提醒 + 每日专注目标检查 -->
        <receiver
            android:name=".notify.LedgerReminderReceiver"
            android:exported="false" />

        <!-- 上课提醒：闹钟到点后重新查库再决定要不要提醒，并顺手排下一节 -->
        <receiver
            android:name=".notify.ClassReminderReceiver"
            android:exported="false" />

        <!-- 重要日期的「提前 N 天提醒」：闹钟到点后重新查库再决定要不要提醒，并顺手排下一次。
             只用 AlarmManager 的非精确闹钟，不申请任何新权限 -->
        <receiver
            android:name=".notify.ImportantDateReminderReceiver"
            android:exported="false" />

        <!-- 每周 / 每月小结 -->
        <receiver
            android:name=".notify.SummaryReceiver"
            android:exported="false" />

        <!-- 重启 / 覆盖安装后，系统会清掉闹钟，这里重新排一遍 -->
        <receiver
            android:name=".notify.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- 桌面小组件：今日支出 / 收入 / 待办 / 专注。
             只用了系统框架 API，没有新增依赖与权限 -->
        <receiver
            android:name=".widget.WidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <!-- 桌面小组件（第二个）：考试倒计时（四六级 / 考研等）。
             同样只用系统框架 API，没有新增依赖与权限；
             「切换考试」是自己给自己发的显式广播（com.dailybook.app.COUNTDOWN_NEXT），
             所以 exported 保持和第一个小组件一致即可 -->
        <receiver
            android:name=".widget.CountdownWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_countdown_info" />
        </receiver>
    </application>

</manifest>
